// Strategies are no longer hard-coded. They are discovered at runtime from the
// `.bat` preset files shipped inside the installed zapret directory and parsed by
// `crate::zapret::batparse`. See `crate::zapret::catalog::LocalStrategyCatalog`.
